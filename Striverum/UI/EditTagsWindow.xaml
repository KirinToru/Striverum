<Window x:Class="Striverum.UI.EditTagsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Striverum"
        xmlns:fa5="http://schemas.fontawesome.com/icons/"
        FocusManager.FocusedElement="{Binding ElementName=TagsBox}"
        mc:Ignorable="d" Background="#181818" WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize" Width="520" SizeToContent="Height">
    <Window.Resources>
        <Style x:Key="ModernTextBoxStyle" TargetType="{x:Type TextBox}">
            <Setter Property="Background" Value="#222222"/>
            <Setter Property="BorderBrush" Value="#383838"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="#F2F2F2"/>
            <Setter Property="CaretBrush" Value="#F2F2F2"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TextBox}">
                        <Border x:Name="Border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="0" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="#555555"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="#D32F2F"/>
                                <Setter TargetName="Border" Property="Background" Value="#262626"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="TagChipStyle" TargetType="{x:Type Button}">
            <Setter Property="Background" Value="#282828"/>
            <Setter Property="BorderBrush" Value="#3E3E3E"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Height" Value="24"/>
            <Setter Property="Padding" Value="8,2"/>
            <Setter Property="Margin" Value="0,0,6,6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="12" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#383838"/>
                                <Setter TargetName="Bd" Property="BorderBrush" Value="#555555"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="18,16,18,18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Margin="0,0,0,16" BorderBrush="#2C2C2C" BorderThickness="0,0,0,1" Padding="0,0,0,10">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <fa5:ImageAwesome Icon="Solid_Tags" Height="15" Width="15" Foreground="#D32F2F" Margin="0,0,8,0" VerticalAlignment="Center"/>
                <TextBlock x:Name="HeaderTitle" FontSize="15" FontWeight="Bold" Foreground="#F2F2F2" Text="Edit Tags" VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- Mod Name -->
        <StackPanel Grid.Row="1" Margin="0,0,0,12">
            <TextBlock Text="Mod Name" FontSize="11" FontWeight="SemiBold" Foreground="#888888" Margin="1,0,0,2"/>
            <TextBlock x:Name="ModNameBlock" FontSize="13" FontWeight="Bold" Foreground="#F2F2F2" TextWrapping="Wrap"/>
        </StackPanel>

        <!-- Tags Input -->
        <StackPanel Grid.Row="2" Margin="0,0,0,10">
            <TextBlock Text="Tags (separated by commas)" FontSize="12" FontWeight="SemiBold" Foreground="#CCCCCC" Margin="1,0,0,6"/>
            <TextBox x:Name="TagsBox" Style="{StaticResource ModernTextBoxStyle}" KeyDown="TagsBox_KeyDown"/>
            <TextBlock Text="Example: Skins, Sol Badguy, Unknown" FontSize="11" Foreground="#666666" Margin="2,4,0,0"/>
        </StackPanel>

        <!-- Suggestions / Quick Add Chips -->
        <StackPanel Grid.Row="3" Margin="0,4,0,18">
            <TextBlock Text="Quick Add / Toggle:" FontSize="11" FontWeight="SemiBold" Foreground="#888888" Margin="1,0,0,6"/>
            <WrapPanel>
                <Button Content="+ Unknown" Style="{StaticResource TagChipStyle}" Click="QuickTag_Click" Tag="Unknown"/>
                <Button Content="+ Skins" Style="{StaticResource TagChipStyle}" Click="QuickTag_Click" Tag="Skins"/>
                <Button Content="+ Translation" Style="{StaticResource TagChipStyle}" Click="QuickTag_Click" Tag="Translation"/>
                <Button Content="+ GUIs" Style="{StaticResource TagChipStyle}" Click="QuickTag_Click" Tag="GUIs"/>
                <Button Content="+ Gameplay" Style="{StaticResource TagChipStyle}" Click="QuickTag_Click" Tag="Gameplay"/>
                <Button Content="+ Stages" Style="{StaticResource TagChipStyle}" Click="QuickTag_Click" Tag="Stages"/>
                <Button Content="+ Sounds" Style="{StaticResource TagChipStyle}" Click="QuickTag_Click" Tag="Sounds"/>
                <Button Content="+ WiPs" Style="{StaticResource TagChipStyle}" Click="QuickTag_Click" Tag="WiPs"/>
            </WrapPanel>
        </StackPanel>

        <!-- Bottom Buttons -->
        <Grid Grid.Row="4">
            <Button Name="ResetDefaultButton" Height="34" HorizontalAlignment="Left" Cursor="Hand" Click="ResetDefaultButton_Click" ToolTip="Reset tags to original categories or 'Unknown'">
                <Button.Template>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="Border" Background="#222222" BorderBrush="#383838" BorderThickness="1" CornerRadius="8" Padding="12,0">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                <fa5:ImageAwesome Icon="Solid_Undo" Height="11" Width="11" Foreground="#A0A0A0" Margin="0,0,6,0"/>
                                <TextBlock Text="Reset to Default" FontSize="12" FontWeight="SemiBold" Foreground="#CCCCCC" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#303030"/>
                                <Setter TargetName="Border" Property="BorderBrush" Value="#555555"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Name="CancelButton" Height="34" Margin="0,0,8,0" Cursor="Hand" Click="CancelButton_Click" IsCancel="True">
                    <Button.Template>
                        <ControlTemplate TargetType="{x:Type Button}">
                            <Border x:Name="Border" Background="#222222" BorderBrush="#383838" BorderThickness="1" CornerRadius="8" Padding="14,0">
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <fa5:ImageAwesome Icon="Solid_Times" Height="11" Width="11" Foreground="#A0A0A0" Margin="0,0,6,0"/>
                                    <TextBlock Text="Cancel" FontSize="13" FontWeight="SemiBold" Foreground="#CCCCCC" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Border" Property="Background" Value="#333333"/>
                                    <Setter TargetName="Border" Property="BorderBrush" Value="#555555"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="Border" Property="Background" Value="#1A1A1A"/>
                                    <Setter TargetName="Border" Property="BorderBrush" Value="#2C2C2C"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
                <Button Name="SaveButton" Height="34" Cursor="Hand" Click="SaveButton_Click" IsDefault="True">
                    <Button.Template>
                        <ControlTemplate TargetType="{x:Type Button}">
                            <Border x:Name="Border" Background="#D32F2F" BorderThickness="0" CornerRadius="8" Padding="16,0">
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <fa5:ImageAwesome Icon="Solid_Check" Height="11" Width="11" Foreground="#FFFFFF" Margin="0,0,6,0"/>
                                    <TextBlock Text="Save Tags" FontSize="13" FontWeight="SemiBold" Foreground="#FFFFFF" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Border" Property="Background" Value="#E53935"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="Border" Property="Background" Value="#B71C1C"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
